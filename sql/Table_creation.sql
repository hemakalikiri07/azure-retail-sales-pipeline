--External table
--Managed tables
--synapse only external table
--get cust data
select * from gold.customers

/*****************************
credential --pick data using manged identity
external data source  -- keep URL until the container level(silve/Adven_cal/) to pic all file in it
external file format  --Define data store in which format(parquet,Jason etc)
*******************************/

--SQL endpoint--